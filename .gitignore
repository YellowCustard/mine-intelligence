# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tooling
.ruff_cache/
.pytest_cache/
.mypy_cache/

# Environment / secrets
.env

# Generated Mosquitto broker credentials (hold credential hashes — never commit).
docker/mosquitto/passwd
docker/mosquitto/acl

# uv
# (uv.lock IS committed — reproducible builds)

# OS
.DS_Store

# Coverage artifacts
.coverage
.coverage.*
htmlcov/
coverage.xml
